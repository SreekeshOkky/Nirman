import React, { useState } from 'react'
import { Building2, Plus, ShieldCheck } from 'lucide-react'
import { useNavigate } from 'react-router-dom'
import { money, PageHeading } from '../components/Shared'
import SiteModal from '../components/SiteModal'
import { api } from '../lib/api'
export default function SitesPage({ sites, setSites, setSiteId, token, flash, isBuilder }) { const [show, setShow] = useState(false); const [saving, setSaving] = useState(false); const navigate = useNavigate(); async function submit(event) { event.preventDefault(); setSaving(true); try { const form = new FormData(event.currentTarget); const data = { name: form.get('name'), location: form.get('location'), client_name: form.get('client_name'), budget: Number(form.get('budget')) || null }; const site = await api.createSite(data, token); setSites(current => [...current, site]); setSiteId(site.id); setShow(false); flash('Site created'); navigate('/') } finally { setSaving(false) } } return <><PageHeading title="Sites" subtitle="Your construction projects, in one place.">{isBuilder && <button className="primary-button" onClick={() => setShow(true)}><Plus size={17} /> New site</button>}</PageHeading><div className="card collection-card"><div className="collection-head"><div><h2>Your construction sites</h2><p>{sites.length} workspace{sites.length === 1 ? '' : 's'}</p></div><span className="secure-label"><ShieldCheck size={14} /> RLS protected</span></div>{sites.length ? <div className="site-grid">{sites.map(site => <button className="site-card" key={site.id} onClick={() => { setSiteId(site.id); navigate('/reports') }}><div className="site-card-top"><span className="site-icon"><Building2 size={18} /></span><span className="status-tag active">{site.status || 'active'}</span></div><h3>{site.name}</h3><p>{site.location || 'Location not added'}</p><div className="site-card-foot"><span>Budget</span><strong>{site.budget ? money(site.budget) : 'Not set'}</strong></div></button>)}</div> : <div className="empty-state">No sites yet. Create your first construction site.</div>}</div>{show && <SiteModal onClose={() => setShow(false)} onSubmit={submit} saving={saving} />}</> }
